---
# BOSSNYUMBA — ExternalSecret pulling app credentials from the active backend.
#
# Backend selection: change `secretStoreRef.name` to point to the active store:
#   - bossnyumba-gcp   (GCP Secret Manager)
#   - bossnyumba-aws   (AWS Secrets Manager)
# For the kubeseal fallback, do NOT apply this manifest — use a SealedSecret CR
# instead (see secret-store-kubeseal.yaml for instructions).
#
# Remote refs assume the secret names below already exist in the backend.
# seed-secrets.sh creates them with empty values that operators rotate via the
# cloud console or CLI. NO REAL SECRETS ARE STORED IN THIS REPO.
#
# Synced Secret object: `bossnyumba-secrets` (Opaque) — referenced by the app
# deployment via `envFrom: [{ secretRef: { name: bossnyumba-secrets } }]`.
#
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: bossnyumba-app
  namespace: bossnyumba
  labels:
    app.kubernetes.io/name: bossnyumba
    app.kubernetes.io/component: external-secrets
spec:
  refreshInterval: 1h
  secretStoreRef:
    # CHANGE per environment: bossnyumba-gcp | bossnyumba-aws
    kind: SecretStore
    name: bossnyumba-gcp
  target:
    name: bossnyumba-secrets
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      type: Opaque
      metadata:
        labels:
          app.kubernetes.io/name: bossnyumba
          app.kubernetes.io/component: app-secrets
  data:
    - secretKey: DATABASE_URL
      remoteRef:
        key: bossnyumba/database-url
        # GCP: omit version => "latest"; AWS: omit version => current AWSCURRENT.
    - secretKey: ANTHROPIC_API_KEY
      remoteRef:
        key: bossnyumba/anthropic-api-key
    - secretKey: OPENAI_API_KEY
      remoteRef:
        key: bossnyumba/openai-api-key
    - secretKey: DEEPSEEK_API_KEY
      remoteRef:
        key: bossnyumba/deepseek-api-key
    - secretKey: MPESA_CONSUMER_SECRET
      remoteRef:
        key: bossnyumba/mpesa-consumer-secret
    - secretKey: MPESA_PASSKEY
      remoteRef:
        key: bossnyumba/mpesa-passkey
