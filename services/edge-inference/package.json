{
  "name": "@bossnyumba/edge-inference",
  "version": "0.1.0",
  "license": "MIT",
  "description": "Cloudflare Workers AI edge-inference scaffold (Roadmap R3). Phase-2 deployment — see Docs/RESEARCH/EDGE_INFERENCE_CLOUDFLARE.md.",
  "main": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240605.0",
    "typescript": "5.4.5",
    "vitest": "4.1.6",
    "wrangler": "^3.66.0"
  }
}
