apiVersion: v1
kind: Service
metadata:
  name: admin-portal
  namespace: bossnyumba
  labels:
    app: admin-portal
    app.kubernetes.io/name: admin-portal
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: admin-portal
