# Jurisdictional-Creep Class Findings
_Generated by `scripts/audit-jurisdictional-creep-coverage.mjs`_
_Phase M-F — Domain-Risk Safeguards_

_Generated at: 2026-05-19T13:30:54.094Z_

This is the *class* counterpart to `audit-jurisdictional-literals.mjs`. It catches branch-on-jurisdiction patterns outside the approved registry.

## Summary

| Class | Count |
|---|---|
| literal-tz-outside-rules | 15 |
| switch-jurisdiction-no-default | 0 |
| country-or-tz-silent-fallback | 27 |
| **Total** | **42** |

## Findings by file

### apps/admin-platform-portal/src/components/ask/SliceSelector.tsx

- `63` — **literal-tz-outside-rules** — `slice.jurisdiction === 'ALL' ? 'all jurisdictions' : slice.jurisdiction,`

### apps/customer-app/src/components/home/HomeSummaryCard.tsx

- `118` — **country-or-tz-silent-fallback** — `(pending as { currency?: string }).currency ?? 'TZS',`

### packages/ai-copilot/src/knowledge/case-studies/index.ts

- `143` — **literal-tz-outside-rules** — `countryCode: cs.country === 'TZ' ? 'TZ' : cs.country === 'KE' ? 'KE' : undefined,`

### packages/ai-copilot/src/rent-credit-building/financing-partnerships.ts

- `71` — **literal-tz-outside-rules** — `if (partner.country !== 'ANY' && partner.country !== config.tenantCountry) continue;`

### packages/central-intelligence/src/kernel/sub-mds/kra-filing-assistant/tools/draft-filing.ts

- `56` — **country-or-tz-silent-fallback** — `const currency = batch.lines[0]?.currency ?? 'KES';`

### packages/central-intelligence/src/kernel/tool-spec/hq-tools/platform.file_kra_mri.ts

- `140` — **literal-tz-outside-rules** — `if (output.jurisdiction === 'KE' && deps.kraEritsDispatcher) {`
- `171` — **literal-tz-outside-rules** — `if ('jurisdiction' in input && input.jurisdiction === 'KE') {`

### packages/domain-models/src/financial/arrears-case.ts

- `249` — **country-or-tz-silent-fallback** — `currency: data.currency ?? 'USD',`

### packages/domain-models/src/financial/invoice.ts

- `249` — **country-or-tz-silent-fallback** — `currency: data.currency ?? 'USD',`

### packages/domain-models/src/financial/receipt.ts

- `233` — **country-or-tz-silent-fallback** — `currency: data.currency ?? 'USD',`

### packages/domain-models/src/financial/transaction.ts

- `271` — **country-or-tz-silent-fallback** — `currency: data.currency ?? 'USD',`

### packages/domain-models/src/legal/case.ts

- `246` — **country-or-tz-silent-fallback** — `currency: data.currency ?? 'USD',`

### packages/domain-models/src/legal/notice.ts

- `270` — **country-or-tz-silent-fallback** — `currency: data.currency ?? 'USD',`

### packages/domain-models/src/payment/payment-plan.ts

- `114` — **country-or-tz-silent-fallback** — `currency: data.currency ?? 'USD',`

### packages/marketing-brain/src/sandbox/sandbox-estate-generator.ts

- `250` — **literal-tz-outside-rules** — `type: country === 'TZ' ? 'tax_filing' : country === 'KE' ? 'fire_safety' : 'license_renewal',`
- `253` — **literal-tz-outside-rules** — `country === 'TZ'`
- `255` — **literal-tz-outside-rules** — `: country === 'KE'`
- `258` — **literal-tz-outside-rules** — `dueBy: daysFromNow(now, country === 'KE' ? -9 : 14),`

### services/api-gateway/src/composition/agency-port-bindings.ts

- `136` — **country-or-tz-silent-fallback** — `const currency = unitRow[0]?.currency ?? 'USD';`

### services/api-gateway/src/composition/move-out-repository.ts

- `71` — **country-or-tz-silent-fallback** — `const currency = checklist.depositReconciliation.currency ?? 'KES';`

### services/api-gateway/src/routes/db-mappers.ts

- `392` — **country-or-tz-silent-fallback** — `currency: row.currency || 'USD',`

### services/api-gateway/src/routes/invoices.ts

- `195` — **country-or-tz-silent-fallback** — `currency: body.currency || 'USD',`

### services/api-gateway/src/routes/payments.ts

- `90` — **country-or-tz-silent-fallback** — `totalDue: { amount: minorToMajor(totalDueMinor), currency: invoices.items[0]?.currency || 'USD' },`
- `93` — **country-or-tz-silent-fallback** — `amount: { amount: minorToMajor(invoice.balanceAmount), currency: invoice.currency || 'USD' },`
- `111` — **country-or-tz-silent-fallback** — `const currency = body.amount?.currency || 'USD';`

### services/api-gateway/src/routes/properties.ts

- `159` — **country-or-tz-silent-fallback** — `country: body.address?.country || 'KE',`

### services/api-gateway/src/routes/work-orders.hono.ts

- `156` — **country-or-tz-silent-fallback** — `currency: body.currency || 'USD',`

### services/api-gateway/src/services/monthly-close/pdf-renderer.ts

- `119` — **country-or-tz-silent-fallback** — ``Currency: ${row.currency || 'XXX'}`,`

### services/api-gateway/src/services/payouts/providers/composite.ts

- `51` — **literal-tz-outside-rules** — `if (input.currency !== 'KES') return false;`

### services/api-gateway/src/services/payouts/providers/mpesa-b2c-adapter.ts

- `280` — **literal-tz-outside-rules** — `if (input.currency !== 'KES') {`

### services/domain-services/src/approvals/approval-service.ts

- `163` — **country-or-tz-silent-fallback** — `r.maxAmountCurrency === (currency ?? 'USD')`

### services/domain-services/src/payment/index.ts

- `458` — **country-or-tz-silent-fallback** — `const currency = input.lineItems[0]?.unitPrice.currency ?? 'USD';`

### services/domain-services/src/property/index.ts

- `585` — **country-or-tz-silent-fallback** — `currency: units.items[0]?.monthlyRent.currency ?? 'USD',`
- `589` — **country-or-tz-silent-fallback** — `currency: units.items[0]?.monthlyRent.currency ?? 'USD',`

### services/domain-services/src/report/index.ts

- `331` — **country-or-tz-silent-fallback** — `const currency = financialMetrics.currency || 'USD';`

### services/notifications/src/services/in-app-notification.service.ts

- `248` — **country-or-tz-silent-fallback** — `const locale = input.locale ?? 'en';`

### services/notifications/src/services/notification.service.ts

- `80` — **country-or-tz-silent-fallback** — `const locale: SupportedLocale = recipient.locale ?? 'en';`

### services/notifications/src/templates/manager.ts

- `29` — **country-or-tz-silent-fallback** — `const loc = locale ?? 'en';`

### services/payments/src/providers/gepg/gepg-client.ts

- `58` — **literal-tz-outside-rules** — `if (req.currency !== 'TZS' && req.currency !== 'USD') {`

### services/payments/src/providers/mpesa/b2c.ts

- `81` — **literal-tz-outside-rules** — `if (params.amount.currency !== 'KES') {`

### services/payments/src/providers/mpesa/stk-push.ts

- `54` — **literal-tz-outside-rules** — `if (params.amount.currency !== 'KES') {`

### services/payments/src/providers/tigopesa/payment.ts

- `58` — **literal-tz-outside-rules** — `if (params.amount.currency !== 'TZS') {`

