'use client';

import { LiveDataRequiredPage } from '@/components/LiveDataRequiredPage';

export default function CollectionsPage() {
  return (
    <LiveDataRequiredPage
      title="Collections"
      feature="collections data"
      description="Sample arrears records, reminders, and payment-plan flows have been removed. This page now requires live collections and communications services."
    />
  );
}
