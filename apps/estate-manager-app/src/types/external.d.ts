// NOTE: @bossnyumba/api-client is a workspace dependency with its own types.
// Do NOT declare it as an ambient module here — that defeats all type-checking.
// If the package fails to resolve, ensure it is built (turbo run build --filter=@bossnyumba/api-client).

declare module '@bossnyumba/design-system/tailwind.config';
