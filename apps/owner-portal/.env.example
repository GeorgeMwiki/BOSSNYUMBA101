# Owner Portal Environment Variables

# API proxy target (used by Vite dev server proxy)
VITE_API_PROXY_TARGET=http://localhost:4000

# API base URL (if not using proxy)
VITE_API_URL=http://localhost:4000
