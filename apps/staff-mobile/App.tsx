import 'expo-router/entry'

// expo-router uses file-based routing rooted at /app. This file exists so that
// tooling that expects an App.tsx entry can resolve, but the actual root is in
// app/_layout.tsx.
export default function App() {
  return null
}
