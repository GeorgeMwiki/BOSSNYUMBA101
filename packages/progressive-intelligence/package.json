{
  "name": "@bossnyumba/progressive-intelligence",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "description": "PI-A — progressive intelligence + auto-fill substrate. Every attribute observation either auto-applies (high-confidence), surfaces an in-chat suggestion (medium), or queues to evidence-pending (low). Every change is tracked, deletions are soft + undoable, history is queryable and replayable. BOSSNYUMBA only — LITFIN is separate.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.6",
    "@types/node": "^25.8.0"
  }
}
