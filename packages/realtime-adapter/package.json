{
  "name": "@bossnyumba/realtime-adapter",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "description": "RealtimePort + adapters for Supabase Realtime (production) and in-process pub/sub (tests). Tenant-scoped channels enforce isolation: convention `tenant.<tenantId>.<topic>` with subscribe-time RLS.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@bossnyumba/supabase-client": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.6",
    "@types/node": "^25.8.0"
  }
}
