{
  "name": "@bossnyumba/anti-scheming",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "description": "Phase N-F: Anti-Scheming + Sleeper-Detection Substrate. Defense-in-depth runtime safeguards: immutable golden eval, external nightly auditor, specification self-correction, adversarial probe injection, sleeper-defection probe, evaluator isolation, monthly red-team rotation. Per Sleeper Agents (Hubinger 2024), Reward Hacking Generalizes (Anthropic 2025), and Apollo Research scheming papers — the eval loop MUST be external to and immutable by the training loop.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^25.8.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.6"
  }
}
