{
  "name": "@bossnyumba/executive-brief-engine",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "description": "Piece C — MD-level executive brief engine. Sensors → hypothesis generation → ToT/LATS verification → debate at HIGH stakes → recommended actions with citations. Reuses kernel primitives.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint src --no-error-on-unmatched-pattern",
    "lint:fix": "eslint src --fix"
  },
  "dependencies": {
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^4.1.10",
    "@vitest/coverage-v8": "^4.1.7",
    "@types/node": "^26.1.1"
  },
  "peerDependencies": {
    "@bossnyumba/org-graph": "workspace:*",
    "@bossnyumba/persona-runtime": "workspace:*"
  }
}
