{
  "name": "@bossnyumba/stage-advisor",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "description": "Stage-aware capability advisor — detects org maturity (pre-launch → ecosystem) from primary signals (units managed) plus secondaries (users, revenue, age, regions), with hysteresis to prevent stage flapping. Surfaces stage-appropriate capabilities, onboarding playbooks, and proactive nudges so the brain introduces the right module at the right moment. Pure functions; injectable triggers sink.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.7",
    "@types/node": "^25.9.1"
  }
}
