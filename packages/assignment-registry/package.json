{
  "name": "@bossnyumba/assignment-registry",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "description": "Assignment registry — ReBAC-style scoped access control. Maps users to specific scopes (parcel/area/property/district/tender/...) with capability lists and expiry. Default-deny scope guard with optional parent-cascade. Append-only assignment-event log for full lifecycle audit.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "dependencies": {
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.8",
    "@types/node": "^25.9.2"
  }
}
