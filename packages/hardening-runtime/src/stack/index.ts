/**
 * Stack module — the M-E composer.
 *
 * Surface:
 *   - `hardenedTurn(input, deps)` — run the full stack end-to-end
 */

export { hardenedTurn } from './hardened-turn.js';
export type { HardenedTurnDeps } from './hardened-turn.js';
