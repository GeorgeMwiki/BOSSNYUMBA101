/**
 * Anomaly-probe module — L3 §8 #14.
 *
 * Surface:
 *   - `probeOutput(text, options?)` — returns AnomalyVerdict
 */

export { probeOutput } from './probe.js';
export type { ProbeOptions } from './probe.js';
