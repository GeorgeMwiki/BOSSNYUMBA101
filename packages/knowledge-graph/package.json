{
  "name": "@bossnyumba/knowledge-graph",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "description": "SOTA-2026 Knowledge Graph + GraphRAG + Graph Visualization spec builders for BOSSNYUMBA — pluggable store ports (in-memory / Kuzu / Neo4j), naive graph embeddings, GraphRAG pipeline (community summary + answerWithKG), bi-temporal facts, PROV-O provenance, real-estate ontology aligned with BOT + RealEstateCore.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.6",
    "@types/node": "^25.8.0"
  }
}
