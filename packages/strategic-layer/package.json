{
  "name": "@bossnyumba/strategic-layer",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "description": "Strategic Direction Layer (Wave M10-M12) — north-star objectives + progress tracking + drift detection + pivot proposals + per-tenant federation consent + per-tenant epsilon-budget accounting (Renyi-DP composition). See Docs/DESIGN/STRATEGIC_DIRECTION_LAYER_SPEC.md §15.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@bossnyumba/audit-hash-chain": "workspace:*",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^25.8.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.6"
  }
}
