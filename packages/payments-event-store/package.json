{
  "name": "@bossnyumba/payments-event-store",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "description": "Append-only event store for the payments-ledger (rent + arrears). LITFIN-parity item 4: audit + replay over the CRITICAL money paths. Closes P64-audit concern #4.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.6",
    "@types/node": "^25.8.0"
  }
}
