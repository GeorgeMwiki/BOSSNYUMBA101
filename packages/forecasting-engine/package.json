{
  "name": "@bossnyumba/forecasting-engine",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "description": "Simulation + forecasting engine — the MD's imagination. World model, sandbox runtime, time-series + causal + stochastic forecasters, scenario library, multi-objective outcome scoring, predicted-vs-actual feedback loop. Pure TypeScript; no Python deps.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.6",
    "@types/node": "^25.8.0"
  }
}
