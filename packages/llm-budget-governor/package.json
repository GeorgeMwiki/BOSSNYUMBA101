{
  "name": "@bossnyumba/llm-budget-governor",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "description": "Per-tenant LLM budget governor with auto-downgrade (opus -> sonnet -> haiku) when approaching cap. Tracks per-tenant + per-period token + dollar spend. Resets when period rolls over. Admin emergency unlocks + per-tenant ceiling adjustments. Ported from LITFIN PROJECT/src/core/mcp/budget.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.6",
    "@types/node": "^25.8.0"
  }
}
