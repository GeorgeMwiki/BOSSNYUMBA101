{
  "name": "@bossnyumba/central-intelligence",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "description": "Embodied agentic intelligence for BossNyumba. The organization (per-tenant) and the industry (platform) speak in first person, grounded in their own knowledge graphs, with tool-using extended-thinking agency.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.5",
    "@types/node": "^25.6.0"
  },
  "peerDependencies": {
    "@bossnyumba/forecasting": "workspace:*",
    "@bossnyumba/graph-privacy": "workspace:*",
    "@bossnyumba/graph-sync": "workspace:*"
  }
}
