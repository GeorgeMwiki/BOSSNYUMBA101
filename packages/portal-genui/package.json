{
  "name": "@bossnyumba/portal-genui",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "description": "Dynamic tab generator — listens to user conversation, detects tab-generation intent (HR, Finance, Compliance, Procurement, ...), uses the multi-LLM synthesizer to draft a PortalTabSchema, and persists it so it shows up on subsequent logins. Extends @bossnyumba/genui PortalLayout with dynamic tabs, fields, and widget factories.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "dependencies": {
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.8",
    "@types/node": "^25.9.2"
  }
}
