/**
 * Module 7 — skill-emit-on-success
 * After every successful task: propose SKILL.md → K-C HITL gate (never auto-promote).
 */

export * from './types.js';
export { proposeSkill, promoteSkill, slugify } from './propose-skill.js';
