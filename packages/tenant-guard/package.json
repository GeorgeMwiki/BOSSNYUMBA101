{
  "name": "@bossnyumba/tenant-guard",
  "version": "0.1.0",
  "private": true,
  "description": "Tenant-isolation guard for BossNyumba. AsyncLocalStorage-based tenant context + Next.js middleware adapter + Drizzle query wrapper + Redis/S3 key-prefix wrappers + log scrubber + audit-chain continuity guard.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "keywords": [
    "tenant-isolation",
    "multi-tenant",
    "security",
    "real-estate",
    "bossnyumba"
  ],
  "exports": {
    ".": "./src/index.ts"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
