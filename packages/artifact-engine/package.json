{
  "name": "@bossnyumba/artifact-engine",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "description": "The ONE unified artifact engine — promotes the portal-genui tab-widget schema to a top-level ArtifactSpec over the 35-kind genui superset, routes any artifact to a surface (inline / canvas) via a pure five-signal AND-gate, and renders every kind through the SAME PART_SCHEMAS dispatch the genui AdaptiveRenderer already uses. Reachability-complete: an unknown kind renders UnknownKindCard, never throws. ADDITIVE — sits ABOVE @bossnyumba/genui (primitives) + @bossnyumba/portal-genui (tab schema); does not replace either.",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./dist/index.mjs",
      "require": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "test": "vitest run --passWithNoTests",
    "test:coverage": "vitest run --coverage --passWithNoTests",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@bossnyumba/genui": "workspace:*",
    "@bossnyumba/portal-genui": "workspace:*",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.2.0",
    "@testing-library/react": "^14.1.2",
    "@types/react": "^18.2.48",
    "@types/react-dom": "^18.2.18",
    "@vitejs/plugin-react": "^4.2.1",
    "jsdom": "^24.0.0",
    "tsup": "^8.0.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.6"
  },
  "peerDependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  }
}
