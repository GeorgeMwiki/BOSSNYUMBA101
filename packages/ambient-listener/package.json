{
  "name": "@bossnyumba/ambient-listener",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "description": "Mr. Mwikila ambient voice listening — consent-gated chat/call/SMS awareness pipeline (Wave 19J). Aware without being creepy: VAD → diarise → STT → redact → extract → persist. Silent disable on any consent gap. Spec: Docs/DESIGN/AMBIENT_VOICE_LISTENING_SPEC.md. Locked default per FOUNDER_LOCKED_DECISIONS_2026_05_26 Decisions 3 + 4.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "dependencies": {
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^25.8.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.6"
  }
}
