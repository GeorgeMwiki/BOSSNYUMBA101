{
  "name": "@bossnyumba/omnidata",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "description": "Omnidata connector framework. Pillar 1 of the capability-boost vision. Owner-consented ingestion of every external source the org uses (Slack, Gmail, Notion, WhatsApp, Drive, Salesforce, social) into Mr. Mwikila's awareness. Wraps @bossnyumba/connectors with sync scheduling, boundary PII redaction, provenance stamping, and audit-hash-chain integration. Concrete connectors land in follow-up waves per source.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@types/node": "^25.8.0",
    "@vitest/coverage-v8": "^4.1.6",
    "typescript": "^6.0.3",
    "vitest": "^4.1.6"
  },
  "peerDependencies": {
    "zod": ">=3.25.0"
  }
}
