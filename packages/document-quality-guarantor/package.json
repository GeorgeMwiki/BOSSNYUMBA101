{
  "name": "@bossnyumba/document-quality-guarantor",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "description": "Document quality guarantor — orchestrates multi-engine OCR intake, multi-engine output generation, blocking quality gates (confidence/schema/citation/roundtrip/visual-diff/font-embedding/accessibility), idempotent retry queue with DLQ, human-in-the-loop escalation to @bossnyumba/workflow-engine, and a WORM-backed audit chain. The package the rest of the system trusts to never silently drop a document.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.6",
    "@types/node": "^25.8.0"
  }
}
