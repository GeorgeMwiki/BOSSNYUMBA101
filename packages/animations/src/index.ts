/**
 * @bossnyumba/animations — centralized Framer Motion + CSS animation tokens.
 *
 * Verbatim port of LitFin's src/core/animations.
 *
 * Public surface:
 *   - spring presets (snappy, smooth, bouncy, gentle)
 *   - transition presets (fadeIn, slideUp, scaleIn, expandWidth)
 *   - CSS transition utility class strings (layoutTransition, colorTransition,
 *     opacityTransition, transformTransition, allFast)
 *   - SPRING_PRESETS, TRANSITION_PRESETS, CSS_TRANSITIONS convenience maps
 */

export * from "./spring-presets";
export * from "./transitions";
