name: bossnyumba_mobile
repository: https://github.com/GeorgeMwiki/BOSSNYUMBA101

packages:
  - packages/**
  - apps/**
  - integration_test

command:
  bootstrap:
    usePubspecOverrides: true

scripts:
  analyze:
    run: melos exec -- dart analyze --fatal-infos
    description: Run dart analyze in all packages

  format:check:
    run: melos exec -- dart format --set-exit-if-changed .
    description: Check dart format in all packages

  format:
    run: melos exec -- dart format .
    description: Format all packages

  test:
    run: melos exec -- flutter test
    description: Run tests in all packages

  test:coverage:
    run: melos exec -- flutter test --coverage
    description: Run tests with coverage in all packages

  build_runner:
    run: melos exec -- dart run build_runner build --delete-conflicting-outputs
    description: Run build_runner in all packages (Drift / Freezed / Riverpod)

  clean:
    run: melos exec -- flutter clean
    description: Clean all packages
