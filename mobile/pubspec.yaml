name: bossnyumba_mobile_workspace
description: >
  BOSSNYUMBA mobile workspace root — coordinates Melos packages and apps.
  Use `melos bootstrap` to install dependencies across the workspace.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.5.0 <4.0.0"

dev_dependencies:
  melos: ^6.2.0
  lints: ^5.0.0
