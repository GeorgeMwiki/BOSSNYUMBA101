# =============================================================================
# mcp-server-opay — default-deny + targeted egress allow-list
# =============================================================================
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: mcp-server-opay-default-deny
  labels:
    app.kubernetes.io/name: mcp-server-opay
    app.kubernetes.io/part-of: bossnyumba
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: mcp-server-opay
  policyTypes: [Ingress, Egress]
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: mcp-server-opay-allow-dns-egress
  labels:
    app.kubernetes.io/name: mcp-server-opay
    app.kubernetes.io/part-of: bossnyumba
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: mcp-server-opay
  policyTypes: [Egress]
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
          podSelector:
            matchLabels:
              k8s-app: kube-dns
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: mcp-server-opay-allow-egress
  labels:
    app.kubernetes.io/name: mcp-server-opay
    app.kubernetes.io/part-of: bossnyumba
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: mcp-server-opay
  policyTypes: [Egress]
  egress:
    # External HTTPS — OPay Merchant API
    - to: []
      ports:
        - protocol: TCP
          port: 443
    # Observability
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: monitoring
      ports:
        - protocol: TCP
          port: 4317
        - protocol: TCP
          port: 4318
