apiVersion: v1
kind: Service
metadata:
  name: sleep-pass-orchestrator
  labels:
    app.kubernetes.io/name: sleep-pass-orchestrator
    app.kubernetes.io/part-of: bossnyumba
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: sleep-pass-orchestrator
  ports:
    # Single port — /metrics shares the app listener.
    - name: http
      port: 80
      targetPort: 3040
      protocol: TCP
