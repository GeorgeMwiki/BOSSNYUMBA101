apiVersion: v1
kind: Service
metadata:
  name: document-intelligence
  labels: { app.kubernetes.io/name: document-intelligence }
spec:
  selector: { app.kubernetes.io/name: document-intelligence }
  ports: [{ name: http, port: 80, targetPort: 3000 }]
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: document-intelligence
  labels:
    app.kubernetes.io/name: document-intelligence
    app.kubernetes.io/component: backend
spec:
  replicas: 2
  selector:
    matchLabels: { app.kubernetes.io/name: document-intelligence }
  template:
    metadata:
      labels:
        app.kubernetes.io/name: document-intelligence
        app.kubernetes.io/component: backend
    spec:
      serviceAccountName: document-intelligence-sa
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        runAsGroup: 1001
        fsGroup: 1001
        seccompProfile: { type: RuntimeDefault }
      containers:
        - name: app
          image: ghcr.io/georgemwiki/bossnyumba-document-intelligence:latest
          ports: [{ containerPort: 3000, name: http }]
          envFrom:
            - configMapRef: { name: bossnyumba-config }
            - secretRef: { name: bossnyumba-secrets }
          env:
            - { name: NODE_ENV, value: production }
            - { name: PORT, value: "3000" }
          resources:
            requests: { cpu: "250m", memory: "256Mi" }
            limits:   { cpu: "1",    memory: "1Gi" }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
          livenessProbe: { httpGet: { path: /healthz, port: 3000 }, initialDelaySeconds: 20, periodSeconds: 15 }
          readinessProbe: { httpGet: { path: /readyz, port: 3000 }, initialDelaySeconds: 5, periodSeconds: 10 }
          volumeMounts: [{ name: tmp, mountPath: /tmp }]
      volumes: [{ name: tmp, emptyDir: { sizeLimit: 512Mi } }]
---
apiVersion: v1
kind: ServiceAccount
metadata: { name: document-intelligence-sa }
automountServiceAccountToken: false
