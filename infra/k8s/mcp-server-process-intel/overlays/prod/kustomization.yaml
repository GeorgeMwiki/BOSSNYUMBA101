apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: bossnyumba-prod

resources:
  - ../../base

images:
  - name: ghcr.io/georgemwiki/bossnyumba-mcp-server-process-intel
    newTag: ${IMAGE_TAG}

patches:
  - target:
      kind: Deployment
      name: mcp-server-process-intel
    patch: |-
      - op: replace
        path: /spec/template/spec/containers/0/resources
        value:
          requests:
            cpu: "500m"
            memory: "768Mi"
          limits:
            cpu: "2"
            memory: "2Gi"
