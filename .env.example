# =============================================================================
# BOSSNYUMBA Environment Configuration — Complete Template
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# First-time production live test: see Docs/SUPABASE_LIVE_TEST.md
# Master docs index:               see Docs/INDEX.md
# Runbooks:                        see Docs/RUNBOOKS/README.md
#
# Sections (top → bottom):
#   [REQUIRED]   — App will not start without these in production
#   [RECOMMENDED] — Core features degrade or run in stub-mode without these
#   [OPTIONAL]   — Feature-flagged; explicit opt-in
#   [LEGACY]     — Documented but slated for removal — do NOT add to new envs
# =============================================================================


# =============================================================================
# === REQUIRED for production live-test ======================================
# =============================================================================

# --- Postgres (Supabase or self-hosted) -------------------------------------
DATABASE_URL=postgresql://bossnyumba:bossnyumba_dev@localhost:5432/bossnyumba
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
SUPABASE_JWT_SECRET=your-supabase-jwt-secret-here
# Physical-bucket env prefix used by `@bossnyumba/storage-adapter`.
# When unset, the wiring falls back to NODE_ENV, then 'development'.
SUPABASE_ENVIRONMENT=development

# --- JWT signing ------------------------------------------------------------
JWT_SECRET=                                   # API gateway HS256 secret (>=32 chars)
JWT_ACCESS_SECRET=                            # Service-to-service RS256 private key
JWT_REFRESH_SECRET=                           # Refresh-token signing secret
JWT_AUDIENCE=bossnyumba
JWT_ISSUER=bossnyumba-api
JWT_EXPIRES_IN=15m

# --- Audit hash-chain (REQUIRED in production) ------------------------------
# Powers packages/ai-copilot/src/security/audit-hash-chain.ts. Generate:
#   openssl rand -base64 48
# _PREV is the optional rotation overlap slot (see Docs/SECRETS_ROTATION.md).
SESSION_HASH_SECRET=replace-me-with-32-byte-base64url-secret
SESSION_HASH_SECRET_PREV=

# --- Field-level encryption (REQUIRED for PII at rest) ----------------------
# See Docs/RUNBOOKS/encryption-at-rest-key-rotation.md.
ENCRYPTION_MASTER_KEY=                        # 32-byte base64 from `openssl rand -base64 32`
ENCRYPTION_MASTER_KEY_PREVIOUS=               # set during rotation overlap window only
ENCRYPTION_KEY_VERSION=1                      # bump on every rotation

# --- CORS + internal API key ------------------------------------------------
ALLOWED_ORIGINS=                              # comma-separated https:// origins
INTERNAL_API_KEY=                             # X-Internal-Key for internal service calls

# --- API URLs (REQUIRED in production) --------------------------------------
API_URL=
API_BASE_URL=
API_GATEWAY_URL=
PUBLIC_API_BASE_URL=                          # base URL inside signed certs (RSP /verify)
PUBLIC_BASE_URL=                              # served in A2A agent-card manifest
FRONTEND_URL=
NEXT_PUBLIC_APP_URL=
NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_API_BASE_URL=
NEXT_PUBLIC_API_GATEWAY_URL=

# --- Node runtime -----------------------------------------------------------
NODE_ENV=development                          # development|test|production
LOG_LEVEL=info                                # trace|debug|info|warn|error|fatal


# =============================================================================
# === RECOMMENDED for production =============================================
# =============================================================================

# --- AI providers (cascade: Claude → OpenAI → DeepSeek) ---------------------
AI_PROVIDER=anthropic
ANTHROPIC_API_KEY=sk-ant-...
ANTHROPIC_BASE_URL=                           # optional endpoint override
CLAUDE_MODEL_DEFAULT=claude-sonnet-4-6-20260217
CLAUDE_MODEL_PREMIUM=claude-opus-4-6-20260210
CLAUDE_MODEL_FAST=claude-haiku-4-5-20251001
OPENAI_API_KEY=sk-proj-...
OPENAI_BASE_URL=
OPENAI_MODEL_DEFAULT=gpt-5.2
OPENAI_VOICE_MODEL=whisper-1
OPENAI_TTS_MODEL=tts-1
OPENAI_IMAGE_MODEL=dall-e-3
OPENAI_REALTIME_URL=
DEEPSEEK_API_KEY=sk-...
DEEPSEEK_BASE_URL=
DEEPSEEK_MODEL_DEFAULT=deepseek-chat

# --- Redis (caching, rate limiting, BullMQ queues) --------------------------
REDIS_URL=redis://localhost:6379
REDIS_HOST=
REDIS_PORT=6379
REDIS_PASSWORD=
UPSTASH_REDIS_REST_URL=https://your-instance.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-token

# --- Stripe (subscription billing) ------------------------------------------
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_CONNECTED_ACCOUNT_ID=                  # Stripe Connect platform account
TZS_USD_EXCHANGE_RATE=2500                    # default fallback FX rate

# --- Mobile Money (Tanzania) ------------------------------------------------
MOBILE_MONEY_ENV=sandbox
MOBILE_MONEY_CALLBACK_URL=https://your-domain/api/webhooks/mobile-money
TANZANIA_PAYMENT_BACKEND=                     # mpesa|tigopesa|airtel|halopesa (unset → factory picks)
PLATFORM_FEE_PERCENT=5.0
CRON_SECRET=                                  # cron-only endpoint guard

# M-Pesa
MPESA_API_KEY=
MPESA_API_SECRET=
MPESA_BUSINESS_SHORT_CODE=
MPESA_CALLBACK_URL=
MPESA_ALLOWED_IPS=                            # comma-separated; defaults to Safaricom prod IPs
MPESA_DISABLE_IP_ALLOWLIST=                   # 'true' bypasses allowlist (dev only)
MPESA_CONSUMER_KEY=
MPESA_CONSUMER_SECRET=
MPESA_SHORT_CODE=
MPESA_SHORTCODE=                              # legacy alias for MPESA_SHORT_CODE
MPESA_PASS_KEY=
MPESA_PASSKEY=                                # legacy alias for MPESA_PASS_KEY
MPESA_ENVIRONMENT=sandbox
MPESA_INITIATOR_PASSWORD=
MPESA_SANDBOX_CERT=
MPESA_PRODUCTION_CERT=
MPESA_CERT_PATH=

# Tigo Pesa
TIGOPESA_API_KEY=
TIGOPESA_API_SECRET=
TIGOPESA_BUSINESS_SHORT_CODE=
TIGOPESA_CALLBACK_SECRET=

# Airtel Money
AIRTELMONEY_API_KEY=
AIRTELMONEY_API_SECRET=
AIRTELMONEY_BUSINESS_SHORT_CODE=
AIRTEL_WEBHOOK_SECRET=

# Halopesa
HALOPESA_API_KEY=
HALOPESA_API_SECRET=
HALOPESA_BUSINESS_SHORT_CODE=
HALOPESA_WEBHOOK_USER=
HALOPESA_WEBHOOK_PASSWORD=

# --- GePG (Tanzania government payment gateway) -----------------------------
GEPG_SP_CODE=
GEPG_SP=
GEPG_SP_SYS_ID=
GEPG_SIGNING_KEY=
GEPG_BASE_URL=https://sandbox.gepg.go.tz
GEPG_MODE=sandbox
GEPG_ENV=sandbox
GEPG_PSP_MODE=true
GEPG_HMAC_SECRET=
GEPG_PUBLIC_CERT_PEM=
GEPG_PKCS12_PATH=
GEPG_PKCS12_PASSWORD=
GEPG_CALLBACK_BASE_URL=
GEPG_HEALTH_URL=
GEPG_SIGNING_KEY_PEM=
GEPG_SIGNING_CERT_PEM=
GEPG_SIGNING_KEY_PATH=
GEPG_SIGNING_CERT_PATH=

# --- Africa's Talking (SMS, voice) ------------------------------------------
AFRICAS_TALKING_API_KEY=
AFRICAS_TALKING_USERNAME=
AFRICAS_TALKING_SENDER_ID=
AFRICAS_TALKING_ENVIRONMENT=sandbox
AFRICASTALKING_WEBHOOK_SECRET=                # HMAC verification for AT delivery reports

# --- Twilio (SMS, WhatsApp, voice, 2FA) -------------------------------------
TWILIO_ACCOUNT_SID=AC...
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_NUMBER=+14155238886
TWILIO_SMS_NUMBER=

# --- WhatsApp Business Cloud API (Meta) -------------------------------------
WHATSAPP_API_URL=https://graph.facebook.com/v18.0
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_BUSINESS_ACCOUNT_ID=
WHATSAPP_WEBHOOK_VERIFY_TOKEN=
META_APP_SECRET=

# --- Email (Resend primary, SendGrid fallback, SMTP backup) -----------------
RESEND_API_KEY=re_...
RESEND_FROM_EMAIL=noreply@bossnyumba.com
RESEND_API_URL=
SENDGRID_API_KEY=
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
NOTIFICATIONS_FROM_EMAIL=
NOTIFICATIONS_FROM_NAME=BOSSNYUMBA
NOTIFICATIONS_SERVICE_URL=

# --- Observability (Sentry + PostHog + OTel) --------------------------------
SENTRY_DSN=
SENTRY_ENVIRONMENT=
SENTRY_TRACES_SAMPLE_RATE=0.1
POSTHOG_API_KEY=
POSTHOG_HOST=https://eu.posthog.com
NEXT_PUBLIC_SENTRY_DSN=
NEXT_PUBLIC_SENTRY_ENVIRONMENT=production
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://eu.posthog.com
OTEL_ENABLED=false                            # set 'true' to enable OTel SDK
OTEL_SERVICE_NAME=bossnyumba-api-gateway
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
OTEL_SAMPLE_RATE=0.1
GIT_SHA=
NEXT_PUBLIC_GIT_SHA=
APP_VERSION=
DEEP_HEALTH_CACHE_MS=15000

# --- Rate limiting ----------------------------------------------------------
RATE_LIMIT_REQUESTS=
RATE_LIMIT_WINDOW_MS=
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_AI_MAX=                            # per-window cap on AI provider calls
TENANT_HOURLY_TOKEN_BUDGET=                   # per-tenant hourly LLM-token ceiling

# --- Storage ----------------------------------------------------------------
STORAGE_BASE_URL=                             # public base URL for signed-download redirects
AWS_S3_BUCKET=                                # primary S3 bucket for blob storage
AWS_REGION=eu-west-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_TEXTRACT_REGION=eu-west-1
CLOUDFLARE_API_TOKEN=                         # used by storage signing helper

# --- Document Intelligence (OCR) --------------------------------------------
OCR_PROVIDER=mock                             # textract|google|mock
GOOGLE_APPLICATION_CREDENTIALS=               # path to Google service-account JSON

# --- Bootstrap + staff access -----------------------------------------------
BOOTSTRAP_SECRET=                             # one-time setup secret
INVITATION_SECRET=                            # signed invitation tokens
STAFF_ADMIN_EMAILS=
STAFF_SUPPORT_EMAILS=
NEXT_PUBLIC_STAFF_ALLOWED_DOMAINS=
STAFF_ALLOWED_DOMAINS=
PLATFORM_ADMIN_EMAILS=
PLATFORM_ADMIN_PASSWORD=
SEED_ADMIN_EMAIL=
SEED_ADMIN_PASSWORD=

# --- Tenant defaults (no hardcoded jurisdiction in business logic) ----------
# Per "Built for the world, starting with TZ" guidance — these only seed
# defaults at tenant-bootstrap time. Business logic reads from the tenant
# record, never from these vars.
DEFAULT_TENANT_COUNTRY=TZ                     # ISO-3166 alpha-2
DEFAULT_TENANT_CURRENCY=TZS                   # ISO-4217
DEFAULT_TENANT_CITY=Dar es Salaam
DEV_DEFAULT_COUNTRY_CODE=
NEXT_PUBLIC_TENANT_ID=
NEXT_PUBLIC_TENANT_COUNTRY=TZ
NEXT_PUBLIC_TENANT_CURRENCY=TZS
NEXT_PUBLIC_TENANT_LOCALE=en-TZ

# --- Support & emergency (customer-facing; no hardcoded fallbacks) ----------
NEXT_PUBLIC_SUPPORT_PHONE=
NEXT_PUBLIC_SUPPORT_WHATSAPP=
NEXT_PUBLIC_SUPPORT_EMAIL=
NEXT_PUBLIC_EMERGENCY_PRIMARY_PHONE=
NEXT_PUBLIC_EMERGENCY_MAINTENANCE_PHONE=
NEXT_PUBLIC_EMERGENCY_SECURITY_PHONE=

# --- Signing secrets (REQUIRED in prod for certs) ---------------------------
CREDIT_CERT_SECRET=
CREDIT_RATING_SIGNING_SECRET=
AGENT_CERT_SIGNING_SECRET=

# --- API key registry -------------------------------------------------------
# Comma-separated entries: "sha256hex:tenantId:role:space-delimited-scopes:serviceName"
# Generate: echo -n "$KEY" | openssl dgst -sha256 -hex | awk '{print $2}'
API_KEY_REGISTRY=
API_KEYS=                                     # DEPRECATED legacy exact-match list

# --- Webhook infrastructure -------------------------------------------------
WEBHOOK_DEFAULT_HMAC_SECRET=
WEBHOOK_SSRF_ALLOW_PRIVATE=                   # 'true' allows private-IP webhook destinations (DEV ONLY)

# --- Outbox + cron supervisors ----------------------------------------------
# See Docs/RUNBOOKS/cron-supervisor-debug.md.
OUTBOX_INTERVAL_MS=5000
OUTBOX_BATCH_SIZE=50
OUTBOX_WORKER_DISABLED=
CASES_SLA_INTERVAL_MS=60000
BOSSNYUMBA_CASES_SLA_DISABLED=                # 'true' disables the case-SLA supervisor
WAKE_LOOP_INTERVAL_MS=                        # wake-loop cron cadence
SOVEREIGN_LEDGER_VERIFY_INTERVAL_MS=          # audit-chain verifier cadence
SESSION_REPLAY_RETENTION_DAYS=30
CONSOLIDATION_INTERVAL_MS=                    # consolidation runner cadence
CONSOLIDATION_PERIOD_KIND=                    # hour|day|week
CONSOLIDATION_WINDOW_DAYS=14
CONSOLIDATION_CYCLE_WINDOW_DAYS=

# --- Background tasks toggles -----------------------------------------------
BOSSNYUMBA_BG_TASKS_ENABLED=                  # 'false' disables all bg tasks (dev)
BOSSNYUMBA_SKIP_DOTENV=                       # 'true' skips .env at api-gateway boot
BOSSNYUMBA_ALLOW_TEARDOWN=                    # 'true' required by scripts/teardown-tenant.ts
BOSSNYUMBA_IDENTITY_WIRED=                    # 'true' once services/identity is wired
BOSSNYUMBA_API_BASE=                          # cross-portal bus discovery target

# --- Durable execution (Inngest + Temporal) ---------------------------------
# See Docs/ADR/0003-inngest-and-temporal-coexistence.md.
INNGEST_EVENT_KEY=
INNGEST_SIGNING_KEY=
# Temporal config inherited from cluster-side config; no env vars here today.

# --- Realtime collaboration (Liveblocks 3.0 rooms) --------------------------
LIVEBLOCKS_SECRET_KEY=

# --- Reports / workers tuning ----------------------------------------------
REPORTS_WORKER_CONCURRENCY=4
SEED_ORG_SEEDS=
SEED_DEMO=
SEED_TENANT_PHONE=
SEED_PROPERTY_CITY=Nairobi

# --- Privacy budget (DP-bounded cohort signals) -----------------------------
PRIVACY_BUDGET_EPSILON=1.0
PRIVACY_BUDGET_DELTA=0.000001

# --- Session replay (front-end opt-in) --------------------------------------
NEXT_PUBLIC_SESSION_REPLAY_ENABLED=false

# --- Next.js server actions encryption --------------------------------------
NEXT_SERVER_ACTIONS_ENCRYPTION_KEY=

# --- Frontend portal URLs ---------------------------------------------------
NEXT_PUBLIC_PLATFORM_PORTAL_BASE_URL=
NEXT_PUBLIC_OWNER_PORTAL_URL=
NEXT_PUBLIC_PDFJS_WORKER_URL=

# --- Feature flags ----------------------------------------------------------
NEXT_PUBLIC_USE_AI_APPRAISAL=true
NEXT_PUBLIC_USE_AI_DESKTOP_REVIEW=true
NEXT_PUBLIC_GIT_BRANCH=
NEXT_PUBLIC_GIT_COMMIT=


# =============================================================================
# === OPTIONAL / feature-flagged =============================================
# =============================================================================

# --- ElevenLabs (voice STT + TTS + conversational AI) -----------------------
ELEVENLABS_API_KEY=
ELEVENLABS_AGENT_ID=
ELEVENLABS_MODEL_STT=scribe_v2
ELEVENLABS_MODEL_TTS=eleven_flash_v2_5
ELEVENLABS_DEFAULT_VOICE_ID=21m00Tcm4TlvDq8ikWAM
ELEVENLABS_API_ENDPOINT=

# --- Neo4j (knowledge graph; falls back to demo mode unset) -----------------
NEO4J_URI=bolt://localhost:7687
NEO4J_URL=bolt://localhost:7687                 # operator-convenience alias
NEO4J_USER=neo4j
NEO4J_USERNAME=neo4j                            # legacy alias
NEO4J_PASSWORD=
NEO4J_DATABASE=neo4j
NEO4J_MAX_POOL_SIZE=50
NEO4J_ACQUISITION_TIMEOUT=60000
NEO4J_CONNECTION_TIMEOUT=30000
NEO4J_MAX_RETRY_TIME=30000

# --- Tanzanian regulatory APIs (production KYC requires partnership) --------
NIDA_API_URL=
NIDA_API_KEY=
NIDA_CLIENT_ID=
NIDA_CLIENT_SECRET=
NIDA_CERTIFICATE=
NIDA_ENVIRONMENT=sandbox
CRB_API_URL=
CRB_API_KEY=
CRB_SUBSCRIBER_ID=
CRB_SUBSCRIBER_PASSWORD=
CRB_ENVIRONMENT=sandbox
CRB_KE_KEY=                                     # Kenya CRB alternate key
BRELA_API_URL=
BRELA_API_KEY=
TRA_API_URL=
TRA_API_KEY=

# --- Hume AI (voice emotion + biomarker detection) --------------------------
HUME_AI_API_KEY=

# --- Azure (speech + translation alternates) --------------------------------
AZURE_SPEECH_KEY=
AZURE_TRANSLATOR_KEY=
AZURE_TRANSLATOR_REGION=eastus

# --- Google (translation, video gen, web search) ----------------------------
GOOGLE_TRANSLATE_API_KEY=
GOOGLE_VEO_API_KEY=
GOOGLE_VEO_BASE_URL=
GOOGLE_KG_API_KEY=
SEARCH_API_KEY=
SEARCH_ENGINE_ID=

# --- Intron (Swahili TTS fallback) ------------------------------------------
INTRON_API_KEY=
INTRON_API_ENDPOINT=https://api.intron.io/v1

# --- Enterprise connectors --------------------------------------------------
# Temenos T24/Transact (Core Banking)
TEMENOS_API_URL=
TEMENOS_API_KEY=
TEMENOS_CLIENT_ID=
TEMENOS_CLIENT_SECRET=
TEMENOS_ENVIRONMENT=sandbox
TEMENOS_RATE_LIMIT=60

# Mambu (Cloud Core Banking)
MAMBU_API_URL=
MAMBU_API_KEY=
MAMBU_ENVIRONMENT=sandbox
MAMBU_RATE_LIMIT=120
MAMBU_WEBHOOK_SECRET=

# CNO
CNO_API_URL=
CNO_API_KEY=
CNO_ENVIRONMENT=sandbox
CNO_RATE_LIMIT=30

# Avoka / Temenos Journey Manager
AVOKA_API_URL=
AVOKA_CLIENT_ID=
AVOKA_CLIENT_SECRET=
AVOKA_ENVIRONMENT=sandbox
AVOKA_RATE_LIMIT=60

# Kony / HCL Volt MX
KONY_API_URL=
KONY_API_KEY=
KONY_APP_KEY=
KONY_APP_SECRET=
KONY_ENVIRONMENT=sandbox
KONY_RATE_LIMIT=100

# Salesforce
SALESFORCE_INSTANCE_URL=
SALESFORCE_CLIENT_ID=
SALESFORCE_CLIENT_SECRET=
SALESFORCE_ENVIRONMENT=sandbox
SALESFORCE_RATE_LIMIT=100
SALESFORCE_API_VERSION=v59.0

# Gmail (deep email integration)
GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
GMAIL_REDIRECT_URI=
GMAIL_RATE_LIMIT=250

# Connector infrastructure
CB_FAILURE_THRESHOLD=5
CB_RESET_TIMEOUT_MS=30000
CB_HALF_OPEN_REQUESTS=3
CONNECTOR_HEALTH_CHECK_INTERVAL_MS=300000
CONNECTOR_MAX_RETRIES=3
CONNECTOR_RETRY_BASE_MS=1000

# --- Knowledge Intelligence Layer ------------------------------------------
KNOWLEDGE_FETCH_ENABLED=true
KNOWLEDGE_AUTO_INGEST=true
KNOWLEDGE_MIN_RELEVANCE=0.6
KNOWLEDGE_MONTHLY_BUDGET=50

# --- Market data adapters --------------------------------------------------
MARKET_DATA_PROVIDER=                          # zillow|airbnb|rentometer|stub
ZILLOW_API_KEY=
AIRBNB_API_KEY=

# --- FX rates feed ---------------------------------------------------------
FIXER_IO_API_KEY=                              # pnpm refresh-fx-rates --provider fixer-io

# --- MCP integration -------------------------------------------------------
# See packages/mcp-server/README.md.
MCP_API_KEY=
MCP_API_KEYS=
MCP_ENABLED=true
MCP_RATE_LIMIT=30
MCP_DEFAULT_TIER=standard
MCP_MONTHLY_BUDGET_USD=500
MCP_HEALTH_CHECK_INTERVAL_S=300

# --- Imagery + PDF generation ----------------------------------------------
NANO_BANANA_API_URL=
NANO_BANANA_API_KEY=
TYPST_BIN=typst                                # path to Typst binary

# --- Tenant context defaults (dev only) ------------------------------------
# Already declared in RECOMMENDED; re-grouped here for discoverability.

# --- E2E / Live Demo testing -----------------------------------------------
BASE_URL=
CUSTOMER_APP_URL=
OWNER_PORTAL_URL=
ADMIN_PORTAL_URL=
ESTATE_MANAGER_URL=
E2E_TEST_OWNER_EMAIL=
E2E_TEST_OWNER_PASSWORD=
E2E_TEST_ADMIN_EMAIL=
E2E_TEST_ADMIN_PASSWORD=
E2E_TEST_MANAGER_EMAIL=
E2E_TEST_MANAGER_PASSWORD=
E2E_TEST_CUSTOMER_PHONE=
E2E_TEST_OTP_CODE=
E2E_GATEWAY_URL=http://localhost:4000
E2E_TEST_JWT=
E2E_REAL_LLM=
USE_MOCK_DATA=false
USE_REAL_SERVERS=
KEEP_TEST_DB=
TEST_DB_HOST=localhost
TEST_DB_PORT=5432
TEST_DB_NAME=bossnyumba_test
TEST_DB_USER=postgres
TEST_DB_PASSWORD=

# --- Vite dev proxy + load testing -----------------------------------------
VITE_API_PROXY_TARGET=http://localhost:4000
LOAD_TEST_PORT=4001
LOAD_TEST_CONNECTIONS=10
LOAD_TEST_DURATION=30
LOAD_TEST_JWT_SECRET=

# --- Admin / customer dev demo creds (optional) ----------------------------
VITE_DEMO_ADMIN_EMAIL=
VITE_DEMO_ADMIN_PASSWORD=
VITE_DEMO_SUPPORT_EMAIL=
VITE_DEMO_SUPPORT_PASSWORD=

# --- Security alerting ------------------------------------------------------
SECURITY_ALERT_EMAIL=security@bossnyumba.com

# --- Operator + migration tooling ------------------------------------------
OPERATOR_ENV=                                  # override NODE_ENV for migration scripts
TGN_INFERENCE_URL=                             # custom TGN inference endpoint

# --- CI / live LLM smoke test ----------------------------------------------
CI_LLM_URL=                                    # endpoint for CI smoke tests against live LLM


# =============================================================================
# === LEGACY / deprecated (remove after wave-M) ==============================
# =============================================================================
# These vars are still read by code paths we are migrating off. Keep set
# while consumers exist; remove from production env once the wave-M
# cleanup PR lands.

# Africa's Talking legacy aliases (prefer AFRICAS_TALKING_*)
AT_API_KEY=
AT_USERNAME=
AT_SENDER_ID=
AT_ENVIRONMENT=sandbox

# Clerk auth (we migrated to Supabase Auth — see ADR 0004)
CLERK_SECRET_KEY=

# Firebase (we migrated off — vestigial in legacy customer-app code path)
FIREBASE_PROJECT_ID=

# Flutterwave (legacy regional payment provider — superseded by direct mobile-money)
FLUTTERWAVE_SECRET_KEY=

# Twilio auth-token alias for legacy notification webhook verifier (use TWILIO_AUTH_TOKEN)
# (no separate var — re-uses TWILIO_AUTH_TOKEN from RECOMMENDED section)
